# Benchmark Roadmap — Frozen Historical Snapshot

> Status: frozen historical background as of 2026-05-02.
>
> 状态：截至 2026-05-02，本文是已冻结的历史背景。

## 决策 / Decision

The old Benchmark roadmap is no longer an active roadmap. Iter E is frozen, the `Iter E 基准冲刺 / Benchmark Ascent` Linear project is canceled, and unfinished Benchmark issues have been canceled.

旧 Benchmark roadmap 不再是活跃路线图。Iter E 已冻结，Linear project `Iter E 基准冲刺 / Benchmark Ascent` 已取消，未完成 Benchmark issues 已取消。

Benchmark is now the lowest project priority. No Iter may write Benchmark code unless the user explicitly asks for Benchmark work.

Benchmark 现在是全项目最低优先级。除非用户明确要求 Benchmark 工作，任何 Iter 都不得编写 Benchmark 代码。

## 历史用途 / Historical Use

This file only records that earlier planning once considered pinned, roadmap, aspirational, and rejected benchmark tiers. Those tiers are not current commitments.

本文只记录早期 planning 曾考虑 pinned、roadmap、aspirational 和 rejected benchmark 分层。这些分层不是当前承诺。

Do not use historical target numbers, SOTA snapshots, leaderboard lists, or Iter schedules from this file to create work. Future Benchmark planning requires an explicit user request, a fresh Linear record, and updated evidence.

不要使用本文中的历史目标数字、SOTA 快照、leaderboard 列表或 Iter 排期来创建工作。未来 Benchmark planning 需要用户明确要求、新的 Linear 记录和更新后的实证。

## 当前可读事实 / Current Readable Facts

Existing code still contains Benchmark implementation surfaces under `benchmarks/` and `providers/memory/benchmarks/`. Those files remain current repository facts, but this roadmap does not authorize extending them.

现有代码仍在 `benchmarks/` 与 `providers/memory/benchmarks/` 下包含 Benchmark 实现面。这些文件仍是当前仓库事实，但本文路线图不授权扩展它们。

The current source of truth is [STATUS.md](../STATUS.md) and the frozen component snapshot in [Benchmark Harness](./README.md).

当前真相源是 [STATUS.md](../STATUS.md) 以及 [Benchmark Harness](./README.md) 中的冻结组件快照。
