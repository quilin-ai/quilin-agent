# Iter E2 R4 Final Independent Review

范围：用户指定 `HEAD f5fbf8b` 加当前未提交 R3 fix pass；本地实测 `git rev-parse HEAD` 为 `ebdc5bb2feebc73eba026d67861e7cd67246c9e3`，复核开始时工作树仅含 R3 fix pass 三个文件：`benchmarks/scripts/docker-smoke.ts`、`benchmarks/src/sandbox/smoke-result.ts`、`benchmarks/src/sandbox/smoke-result.test.ts`。本轮未执行 `commit/push/fetch/checkout/stash` 等 git 写命令。

结论：**Iter E2 收口：YES**。R3 HIGH-1 已闭合；本轮未发现 BLOCKING / HIGH / MEDIUM / LOW finding。macOS 本机无法执行真实 Linux Docker smoke，按要求只记录 structured skip，不伪造 Linux 结果。

## Evidence Run

- `code-review-graph detect_changes(base=HEAD, changed_files=R3 fix pass)`：3 changed files，overall risk `0.00`；graph 对两个新文件未给出函数级节点，但 `get_review_context` 成功给出源码片段。
- `code-review-graph get_review_context(base=HEAD, changed_files=R3 fix pass)`：1 directly changed node，41 impacted nodes；重点集中在 benchmark runner / sandbox import surface。
- `git status --short`：`M benchmarks/scripts/docker-smoke.ts`，`?? benchmarks/src/sandbox/smoke-result.ts`，`?? benchmarks/src/sandbox/smoke-result.test.ts`。
- `pnpm --filter @quilin/benchmarks exec vitest run src/sandbox/smoke-result.test.ts --configLoader runner`：1 file passed，4 tests passed。
- `pnpm --filter @quilin/benchmarks exec vitest run src/sandbox/smoke-result.test.ts --coverage --configLoader runner`：helper focused coverage 100% statements / branches / functions / lines。
- Direct helper probe：`isError`、nonzero `exitCode`、`timedOut`、`output_truncated` all fail-closed under `assertDockerSmokeCommandSucceeded()`.
- `pnpm --filter @quilin/benchmarks build`：pass。
- `pnpm --filter @quilin/benchmarks lint`：pass，Biome checked 41 files。
- `pnpm --filter @quilin/benchmarks test`：15 files passed，201 passed / 1 skipped。
- `pnpm --filter @quilin/benchmarks exec vitest run --coverage --configLoader runner`：pass；statements `98.39%`，branches `95.81%`，functions `98.92%`，lines `98.38%`。
- `just test-all`：pass；TS `717 passed`，Python `187 passed` with total coverage `95.28%`，Rust `1 passed`。
- `cd providers/memory && uv run python benchmarks/amb_100k.py --cache-path .bench-cache/amb_100k-r4.sqlite`：pass；100000 records，top1 `1.0`，p95 `0.314ms`。
- `cd benchmarks && bun run scripts/docker-smoke.ts`：macOS structured skip，`{"event":"docker_smoke_skipped","reason":"macos-no-docker","platform":"darwin","status":"skipped"}`。

## BLOCKING

None.

## HIGH

None.

## MEDIUM

None.

## LOW

None.

## R3 HIGH-1 Closure

R3 HIGH-1 要求 Docker smoke 不再忽略 sandboxed `shell_exec` 的 tool result，并确保隔离断言失败会让 smoke fail。当前实现满足：

- `benchmarks/scripts/docker-smoke.ts:87-107` 保存 `const shellResult = await shellExecTool.execute(...)`，随后调用 `assertDockerSmokeCommandSucceeded(shellResult, "DockerSandbox isolation smoke")`，不再先返回 patch。
- `benchmarks/src/sandbox/smoke-result.ts:14-27` 对 success probe 同时要求 `isError === false`、`exitCode === 0`、`timedOut === false`、`output_truncated === false`。任一不满足都会 throw，并在错误消息里带上 `isError`、`exitCode`、`timedOut`、`output_truncated`、`stderr`、`stdout`。
- `benchmarks/src/sandbox/smoke-result.ts:56-98` 严格校验 tool envelope 和 JSON payload：非对象、缺 `content`、缺 `isError`、invalid JSON、non-object JSON、字段不完整都会 fail。
- `benchmarks/src/sandbox/smoke-result.ts:30-38` timeout probe 要求 tool result 是 `isError=true` 且 `timedOut=true`，避免非 timeout 正常返回误报为 timeout enforced。

目录和网络 probe 也已补齐：

- `benchmarks/scripts/docker-smoke.ts:89-93` 明确 `test -d /workspace/base`、`/workspace/cache`、`/workspace/task`、`/workspace/artifacts`。
- `benchmarks/scripts/docker-smoke.ts:97-98` 继续验证 base/cache 只读，写入成功分别以 exit `10` / `11` fail。
- `benchmarks/scripts/docker-smoke.ts:99` 对 `command -v wget` 缺失给出明确 `wget-missing` 和 exit `13`，不会被误报为 network denied。
- `benchmarks/scripts/docker-smoke.ts:100-101` 只有 `wget` 存在且 outbound 失败后才写 `network-denied.txt`；若网络意外允许则 exit `12` 并由 helper fail。
- `benchmarks/scripts/docker-smoke.ts:128-137` timeout smoke 继续执行真实 sandbox `sleep 60` with `timeoutMs: 100`，并用 helper 检查 timed-out tool error。

测试充分性：

- `benchmarks/src/sandbox/smoke-result.test.ts:17-24` 覆盖 ignored tool error + parseable JSON 的 R3 false-positive 核心路径。
- `benchmarks/src/sandbox/smoke-result.test.ts:26-59` 覆盖 truncated output、null result、missing content、missing `isError`、invalid JSON、non-object JSON、incomplete payload。
- `benchmarks/src/sandbox/smoke-result.test.ts:62-74` 覆盖 timeout assertion 的 pass/fail 两侧。
- Focused helper coverage 为 100% branches；额外 direct probe 验证 `isError`、nonzero exit、timeout、truncation 四类 success-probe failure 均 fail-closed。

## Decision

Iter E2 收口：**YES**。

当前 DoD 状态：coverage >=95% yes；build/lint/test yes；`just test-all` yes；AMB p95 <=300ms yes；macOS docker-smoke structured skip yes；BLOCKING=0 yes；HIGH=0 yes。
