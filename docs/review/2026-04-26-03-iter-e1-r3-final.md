# Iter E1 R3 终审报告

## Summary

R3 结论：**Iter E1 收口：yes**。

范围：`7b633cb..52208b6`。本轮独立复核 R1 报告、R1 fix、R2 报告、R2 fix，不复用 R1/R2 结论。

DoD 状态：

- BLOCKING：0
- HIGH：0
- MEDIUM：0
- LOW：1
- Benchmark coverage：Statements `98.62%` / Branches `95.29%` / Functions `100%` / Lines `98.61%`
- `just test-all`：TS `717` passed + Python `187` passed + Rust `1` passed
- AMB 100k：top3 `1.0`，p95 `181.98ms`，max `183.162ms`，低于 `300ms`

## Evidence

- `code-review-graph detect_changes(base=7b633cb)`：13 changed files，53 changed functions/classes，risk score `0.55`；优先点集中在 runner path containment、cache realpath、SWE scorer factory。
- `code-review-graph get_review_context(base=7b633cb)`：13 changed files，138 directly changed nodes，496 impacted nodes in 76 files；提示 runner/cache/scorer test gaps。
- `code-review-graph list_graph_stats`：249 files，2659 nodes，26532 edges；last updated `2026-04-26T01:21:12`。
- `git rev-parse --short HEAD`：`52208b6`。
- `git show --stat --oneline --decorate 7b633cb..52208b6`：3 commits：`5ba2b69` R1 fix、`6dddbd6` R2 report、`52208b6` R2 fix。
- `git diff --stat 7b633cb..52208b6`：13 files changed，1043 insertions / 74 deletions。
- `git diff --check 7b633cb..52208b6`：exit 0。
- `pnpm -C benchmarks build`：exit 0。
- `pnpm -C benchmarks exec biome check src scripts vitest.config.ts`：32 files checked，no fixes applied。
- `cd benchmarks && pnpm exec vitest run src/datasets/swe-bench-lite.test.ts src/runner/runner.test.ts src/scorers/swe-bench-patch-apply.test.ts --configLoader runner`：3 files / 58 tests passed。
- `cd benchmarks && pnpm exec vitest run --coverage`：11 files / 129 tests passed；coverage Stmt `98.62%` / Branch `95.29%` / Func `100%` / Lines `98.61%`.
- `just test-all`：exit 0；agent-core TS `717` passed，memory Python `187` passed / TOTAL coverage `95.28%`，Rust `1` passed。
- `cd providers/memory && uv run python benchmarks/amb_baseline.py --sample-size 100000 --iterations 30`：top3 `1.0`，p95 `181.98ms`，max `183.162ms`，local-only / network_calls `0` / vector_calls `0` / kg_calls `0`。

## Findings

### BLOCKING

None.

### HIGH

None.

### MEDIUM

None.

### LOW

#### LOW-1. realpath/lstat containment 仍是 check-then-use，不是 descriptor-level TOCTOU proof

证据：

- Cache：`loadDatasetCache()` 先调用 `resolveContainedDataPath()` / `assertExistingPathContained()`，随后才用原始 `dataPath` 执行 async `readFile()`：`benchmarks/src/datasets/cache.ts:56-57`、`benchmarks/src/datasets/cache.ts:85-109`、`benchmarks/src/datasets/cache.ts:152-154`。
- Runner：`assertPathContained()` 先对 candidate 做 lexical containment + nearest existing path realpath，随后把原始 args 交给 tool：`benchmarks/src/runner/runner.ts:379-380`、`benchmarks/src/runner/runner.ts:472-503`、`benchmarks/src/runner/runner.ts:528-550`。

影响：

R2 的 symlink escape 已关闭，但实现仍不是不可竞争的打开方式。如果 cache/workspace 目录被同机并发进程在检查和读取/工具使用之间替换 symlink，理论上仍有 TOCTOU 窗口。这里没有升级为 MEDIUM/HIGH，因为当前 E1 目标是拒绝静态恶意 manifest/tool args；并发写入同一个 per-task tmpdir/cache root 本身已超出本轮 Docker/OS namespace 缺失时的隔离能力。建议在 Iter F DockerSandbox 或更强本地 sandbox 中用 descriptor/open-time 约束（例如 no-follow/open fd 后读，或禁止 symlink + locked cache transaction）收掉。

## R1 Closure Check

- `B1 default runner/scorer lazy imports private TS source subpaths`：已关闭。`rg -n "@quilin/agent-core|src/loop\\.js|src/tools/builtin/shell-exec\\.js|workspace:\\*|sweBenchPatchApplyScorer|defaultGitApplyCheckExecutor|createSweBenchPatchApplyScorer\\(\\)" benchmarks pnpm-lock.yaml` 无结果；`benchmarks/package.json:13-15` 只剩 `zod` 运行时依赖；runner 要求 injected `runAgent`（`benchmarks/src/runner/runner.ts:85-92`、`349-356`）。
- `B2 cwd containment / filesystem sandbox not enforced`：已关闭到 E1 契约要求。runner 包装 config tools（`benchmarks/src/runner/runner.ts:359-381`），为 `shell_exec` 注入 cwd（`398-400`），检查 path-like fields 和 command path tokens（`425-464`、`472-503`），并有真实正负测试（`benchmarks/src/runner/runner.test.ts:305-355`、`385-501`）。
- `H1 real shell_exec scorer path denied / unverified`：已关闭。默认 deep import executor 被删除；`createSweBenchPatchApplyScorer()` 现在 factory-time 强制注入 executor（`benchmarks/src/scorers/swe-bench-patch-apply.ts:35-47`），shell_exec bridge 保留为显式 composition helper（`98-120`），测试覆盖工厂失败和 shell_exec executor（`benchmarks/src/scorers/swe-bench-patch-apply.test.ts:165-213`）。
- `H2 cache data_file replacement`：已关闭。cache 只允许 `data.jsonl` / `data.json` canonical filename（`benchmarks/src/datasets/cache.ts:7`、`68-82`），拒绝 `../`、绝对路径、非 canonical replacement 和 matching-sha `evil.jsonl`（`benchmarks/src/datasets/swe-bench-lite.test.ts:167-213`）。
- `M1 ADR submission format`：已关闭。ADR 明确区分本地 harness JSONL 与 `sb-cli` JSON dict/list，且说明 E2 需按 submit 入口做独立 adapter（`docs/adr/adr-010-benchmark-harness-wire-schema.md:133-135`）。
- `M2 score clamp vs leaderboard-specific`：本轮范围未改 result wire；R1 没把它列入必须修的闭合项，当前 E1 SWE patch-apply 仍为 `[0,1]` 分数。后续 GAIA/BFCL 如需非 `[0,1]`，应在对应 leaderboard adapter/scorer 设计时处理。
- `L1 write boundary drift`：已关闭。`benchmarks/package.json:13-15` / `pnpm-lock.yaml` 无 `@quilin/agent-core` workspace dependency；`git diff 7b633cb..52208b6 -- docs/planning/2026-04-25-03-iter-e1-restart.md docs/planning/00-implementation-plan.md docs/planning/2026-04-23-01-iter-c-m-parallel-breakdown.md docs/planning/2026-04-25-02-c-m-cleanup-sweep.md` 为空。
- `L2 coverage misses highest-risk paths`：已关闭。新增 tests 覆盖 injected runner requirement、real tool wrapper、network whitelist、cache replacement、cache symlink escape、workspace symlink escape、Windows absolute paths、factory-only scorer。

## R2 Closure Check

- `HIGH-1 Cache/runner filesystem containment symlink/realpath escape`：已关闭。cache 使用 `lstatSync` + `realpathSync.native` 双侧 containment（`benchmarks/src/datasets/cache.ts:85-109`）；runner 对 workspace root 和 nearest existing candidate 做 `realpathSync.native`（`benchmarks/src/runner/runner.ts:528-550`）。测试使用真实 `fs.symlinkSync`：cache `data.jsonl -> outsideRoot/data.jsonl`（`benchmarks/src/datasets/swe-bench-lite.test.ts:215-231`），runner `workspace/link-out -> outsideDir`（`benchmarks/src/runner/runner.test.ts:423-464`）。
- `MEDIUM-1 factory-only scorer DX trap`：已关闭。`sweBenchPatchApplyScorer` singleton 和 `defaultGitApplyCheckExecutor` 不再 export（`benchmarks/src/scorers/index.ts:6-15`）；factory 强制 executor（`benchmarks/src/scorers/swe-bench-patch-apply.ts:39-47`）；`rg` 未发现默认 singleton/import 误用。Registry 真实路径没有被破坏：registry 仍接受任意 `Scorer`（`benchmarks/src/scorers/registry.ts:14-37`），runner 仍通过 `scorerRegistry.get(task.scorer_type)` 路由（`benchmarks/src/runner/runner.ts:659-669`），测试验证 scorer_type routing（`benchmarks/src/runner/runner.test.ts:144-169`）。

## Targeted Risk Checks

- Symlink fixture 真实性：通过。cache negative test 用 `fs.symlinkSync`，目标是独立 `mkdtemp(tmpdir(), "quilin-benchmarks-outside-")` 下的 `data.jsonl`，不在 cache root 内（`benchmarks/src/datasets/swe-bench-lite.test.ts:215-227`）。runner negative test 用真实 `symlinkSync(outsideDir, join(workspaceDir, "link-out"), "dir")`（`benchmarks/src/runner/runner.test.ts:423-464`）。
- macOS `/var` ↔ `/private/var`：cache 双侧对齐。`cacheDir` 和 `dataPath` 都在 containment 判定前走 `realpathSync.native`（`benchmarks/src/datasets/cache.ts:102-104`），能把 macOS `/var` alias 归一到 `/private/var`。runner realpath 阶段也双侧对齐（`benchmarks/src/runner/runner.ts:533-545`）；absolute alias 在前置 lexical check 仍可能被保守拒绝，但不会放行逃逸。
- factory-only scorer wire/registry：未发现破坏。wire 的 `scorer_type` 仍是字符串路由；registry 不依赖默认 singleton；真实注册方需要显式创建 scorer 并注入 executor。
- Plan §17 残余归属：仍稳。`docs/planning/00-implementation-plan.md:542-550` 继续把 M1.1 / M0.9b、LongMemEval、Arm L gate 列为资源 blocked/待激活；`docs/planning/2026-04-23-01-iter-c-m-parallel-breakdown.md:983-989` 同步；`docs/planning/2026-04-25-02-c-m-cleanup-sweep.md:64-80` 显示 25-02 sweep 已闭合且未把这些资源 blocked 项提前拉入 E1。

## Decision

**Iter E1 收口：yes**。

理由：R1/R2 的 BLOCKING/HIGH/MEDIUM closure 项均有代码和测试实证闭合；新增风险只有 LOW 级 TOCTOU hardening 残留，属于 Docker/OS namespace 缺失条件下的后续加固，不阻塞 E1 收口。
